# SPlisHSPlasH - Cylinder In Fluid

This case uses SPlisHSPlasH's JSON scene runner with a dynamic cylinder rigid body inside an SPH fluid block.

- `run.sh --smoke` runs a one-step headless scene for external-solver startup validation.
- `run.sh --full` runs a longer headless scene with the same cylinder setup at the finer particle radius.
