# CLAUDE.md — Atlas Engine

This file is a **map**. It does not hold the full instructions; it points to the
documents that do. When contributing to Atlas Engine, read the relevant linked
document before changing code, and keep that document in sync when the change is
intentional.

The detailed material lives in two directories:

- [`docs/architecture/`](docs/architecture/) — *what the program is*: structure,
  runtime objects, the per-step pipeline, the backend model, the module map, and
  structural conventions. This is the source of truth for program structure.
- [`docs/guidelines/`](docs/guidelines/) — *how to work on it*: workflow, code
  style, build/test, and dependencies.

## Always Applies

A few rules are easy to miss; everything else is in the linked docs.

- Respond in Korean unless the user asks for another language.
- Do not run builds, tests, benchmarks, simulations, generators, or formatters
  unless the user explicitly asks.
- Do not write source-code comments unless explicitly requested; when
  requested, write them in English.
- `include/atlas/atlas.h` is generated by `tools/generate_headers.py` — do not
  hand-edit it.
- Base code changes on `docs/architecture/`; when you make an intentional
  architectural change, update the affected document in the same change.

## Map

### Working on the code → `docs/guidelines/`

| I want to… | Read |
|---|---|
| Know the working agreement (language, change scope, when to ask, git/commit) | [guidelines/workflow.md](docs/guidelines/workflow.md) |
| Write code that fits the project (scope, structure, naming, state, comments) | [guidelines/coding-style.md](docs/guidelines/coding-style.md) |
| Build, configure presets, or author tests | [guidelines/build-and-test.md](docs/guidelines/build-and-test.md) |
| Check dependencies or benchmark reference submodules | [guidelines/dependencies.md](docs/guidelines/dependencies.md) |

### Understanding the program → `docs/architecture/`

| I want to… | Read |
|---|---|
| See the layered design and source-tree map | [architecture/01-overview.md](docs/architecture/01-overview.md) |
| Learn the runtime objects and ownership | [architecture/02-runtime-objects.md](docs/architecture/02-runtime-objects.md) |
| Follow a simulation step end to end | [architecture/03-simulation-pipeline.md](docs/architecture/03-simulation-pipeline.md) |
| Understand the CUDA/TBB backend model | [architecture/04-backend-portability.md](docs/architecture/04-backend-portability.md) |
| Look up a specific module directory | [architecture/05-module-map.md](docs/architecture/05-module-map.md) |
| Check a structural convention (files, builders, naming, solver rules) | [architecture/06-conventions.md](docs/architecture/06-conventions.md) |
| Add a new geometry/solver/kernel (extension points, `visit` recipe) | [architecture/07-extensibility.md](docs/architecture/07-extensibility.md) |

Each directory also has its own `README.md` index:
[`docs/architecture/README.md`](docs/architecture/README.md) and
[`docs/guidelines/README.md`](docs/guidelines/README.md).
